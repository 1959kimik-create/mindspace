명상 웹앱 MVP 상세 기획안
________________________________________
1. 프로젝트 개요
•	서비스명 (가칭): MindSpace (마인드스페이스)
•	목표: 로그인 없이 접속 즉시 차분한 미디어 환경에서 명상하고, 완료 기록을 달력으로 확인하는 단일 페이지 웹앱(SPA)
•	주요 타겟: 복잡한 과정 없이 빠르게 5~10분 명상을 즐기고 싶은 직장인 및 학생
________________________________________
2. 핵심 기능 상세 명세 (MVP Scope)
•	시간대별 자동 배경 테마 (수동 선택 포함)
o	시간 조건:
	아침(06:00~10:00) / 낮(10:00~17:00) / 저녁(17:00~20:00) / 밤(20:00~01:00) / 새벽(01:00~06:00)
o	동작: 접속 시 사용자 기기 시간(System Clock)을 읽어 배경 비디오/이미지와 UI 분위기 자동 전환
o	수동 전환: 상단 테마 선택 드롭다운/아이콘을 두어 접속 시간과 무관하게 원하는 분위기 변경 가능
•	유튜브 명상 플레이리스트 임베드
o	인터페이스: 어두운 톤의 카드로 카테고리(수면, 집중, 휴식, 자연의 소리)별 영상 제공
o	재생 방식: 화면 내 <iframe> 플레이어 또는 배경에 숨김 처리된 유튜브 API 연동으로 이탈 없는 웹 내 재생
•	명상 타이머 & 완료 스탬프
o	타이머 옵션: 3분, 5분, 10분, 15분, 자율(무제한)
o	종료 트리거: 타이머 종료 시 은은한 알람음(오디오 파일) 재생 및 "오늘의 명상 완료!" 모달 팝업 출현
•	LocalStorage 기반 챌린지 달력
o	저장 데이터: [ {"date": "2026-09-13", "duration": 10} ] 형태로 브라우저 로컬 저장소에 저장
o	달력 UI: 이번 달 달력 뷰에 명상 완료한 날짜는 특정 스탬프 아이콘/색상 표시, 클릭 시 당일 명상 시간 노출
o	주의 안내: 하단에 "브라우저 쿠키/방문 기록 삭제 시 기록이 초기화될 수 있습니다" 문구 포함
________________________________________
3. 화면 구성 및 레이아웃 (UI/UX Design)
•	전체 UI 톤: 다크 모드 기본 (#121212 / #1E1E2C), 반투명 글래스모피즘(Glassmorphism) 스타일 적용
+-----------------------------------------------------------------------+
| [MindSpace]               [시간대 테마 선택 ▾]  [달력 보기 📅]        |
|-----------------------------------------------------------------------|
|                                                                       |
|                       "오늘 하루도 수고 많으셨습니다."                   |
|                            [ 10:00 ] (타이머)                          |
|                           [ ▶ 명상 시작 ]                             |
|                                                                       |
|-----------------------------------------------------------------------|
| [ 추천 명상 음악 카테고리 ]                                            |
|  +--------------+  +--------------+  +--------------+                 |
|  | 🌙 수면 명상 |  | 🌲 숲속 소리 |  | 🌊 파도 소리 |                 |
|  +--------------+  +--------------+  +--------------+                 |
+-----------------------------------------------------------------------+
________________________________________
4. 데이터 구조 및 기술 스택
•	기술 스택: Vanilla HTML5, CSS3, Pure JavaScript (별도 프레임워크 없이 단일 파일 구조로 빠르게 구축 가능)
•	데이터 관리 방식 (LocalStorage 예시):
JavaScript
// 저장 로직 예시
const logMeditation = (minutes) => {
  const today = new Date().toISOString().split('T')[0];
  let logs = JSON.parse(localStorage.getItem('meditationLogs')) || [];
  logs.push({ date: today, duration: minutes });
  localStorage.setItem('meditationLogs', JSON.stringify(logs));
};
________________________________________
5. MVP 개발 및 테스트 단계 (Action Plan)
•	1단계 (기본 틀): HTML/CSS로 어두운 UI 레이아웃 및 시간대별 배경 적용
•	2단계 (타이머 & 미디어): JavaScript 타이머 및 유튜브 임베드 플레이어 연동
•	3단계 (기록 & 달력): LocalStorage 연동 및 달력 라이브러리(예: FullCalendar 또는 순수 JS 달력) 스탬프 표시 구현
•	4단계 (배포): GitHub Pages 또는 Vercel을 통한 무료 웹 호스팅 배포
